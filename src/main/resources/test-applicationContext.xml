<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="dataSource"
          class="org.springframework.jdbc.datasource.SimpleDriverDataSource">
        <property name="driverClass" value="com.mysql.jdbc.Driver" />
        <property name="url" value="jdbc:mysql://localhost/study" />
        <property name="username" value="dragon" />
        <property name="password" value="1234" />
    </bean>

    <bean id="userService" class="springbook.user.service.TxProxyFactoryBean">
        <!-- target, trasactionManager 다른 빈을 가리키므로 ref 애트리뷰트 사용 -->
        <property name="target" ref="userServiceImpl" />
        <property name="transactionManager" ref="transactionManager" />
        <!-- pattern은 스트링으로 된 문자열이니 value 사용해 값을 지정 -->
        <property name="pattern" value="upgradeLevels" />
        <!-- serviceInterface가 Class타입이므로 value를 이용해 클래스 또는 인터페이스 이름을 지정, 스프링이 자동으로 class 오브젝트로 변환 해줌 -->
        <property name="serviceInterface" value="springbook.user.service.UserService" />
    </bean>

    <bean id="userServiceImpl" class="springbook.user.service.UserServiceImpl">
        <property name="userDao" ref="userDao"/>
        <property name="mailSender" ref="mailSender"/>
    </bean>

    <bean id="transactionManager"
        class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <bean id="mailSender" class="springbook.user.service.DummyMailSender" />

    <bean id="userDao" class="springbook.user.dao.UserDaoJdbc">
        <property name="dataSource" ref="dataSource" />
    </bean>

</beans>


